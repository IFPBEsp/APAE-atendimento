name: SonarQube Analysis

on:
  push:
    branches: [ "dev", "main" ]
  pull_request:
    branches: [ "dev", "main" ]

jobs:
  call-sonar-fullstack:
    uses: IFPBEsp/APAE-INFRA/.github/workflows/reusable-sonarqube-fullstack.yml@dev
    with:
      backend-project-key: "apae-atendimento-backend"
      frontend-project-key: "apae-atendimento-frontend"
      backend-dir: "backend/atendimento"
      frontend-dir: "frontend/atendimento-app"
      organization: "ifpbesp"
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}